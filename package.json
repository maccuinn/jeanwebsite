{
  "name": "jeanwebsite",
  "version": "1.0.0",
  "description": "",
  "main": "\"\"",
  "scripts": {
    "postinstall": "bower install && npm run scss",
    "test": "echo \"Error: no test specified\" && exit 1",
    "scss": "node-sass -o styles ./sass/styles.scss --source-map=true",
    "scss:watch": "node-sass -o styles ./sass/styles.scss --source-map=true --watch --recursive",
    "deploy": "gcloud app deploy --quiet --project ingeniuscreations-ca"
  },
  "repository": {
    "type": "git",
    "url": "\"\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@webcomponents/webcomponentsjs": "^2.2.10",
    "bower": "^1.8.8",
    "jquery": "^3.4.1",
    "node-sass": "^4.12.0"
  }
}
